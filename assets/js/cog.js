$(document).ready(function() {
    $('#settings').bind('click', showSettings);
});

// The function showSettings drops down a lightbox with the settings
// panel inside so users can update their settings.
function showSettings() {
    
}